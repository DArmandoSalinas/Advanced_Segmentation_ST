[server]
# Increase max upload size to 500MB (default is 200MB)
maxUploadSize = 500

# Increase max message size
maxMessageSize = 500

# Enable CORS (needed for Cloud Run)
enableCORS = false
enableXsrfProtection = false

[runner]
# Increase memory limit for file processing
magicEnabled = true
